# Fly.io app config for the шукайпес API.
# Lives at repo root so the Docker build context includes app/ + shared/ + server/.
# First-time deploy (from repo root):
#   fly launch --no-deploy --copy-config --name shukajpes-api
#   fly secrets set DATABASE_URL=... REDIS_URL=... ANTHROPIC_API_KEY=... GOOGLE_MAPS_API_KEY=...
#   fly deploy

app = "shukajpes-api"
primary_region = "fra"  # Frankfurt — low-latency for Kyiv users.

[build]
  dockerfile = "server/Dockerfile"

[env]
  NODE_ENV = "production"
  PORT = "3000"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1  # Keep one warm so cron decay ticks actually run.
  processes = ["app"]

  [[http_service.checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "10s"
    method = "GET"
    path = "/health"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
