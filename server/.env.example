# server config
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# database (Phase 3)
DATABASE_URL=postgresql://shukajpes:shukajpes@localhost:5432/shukajpes_dev
REDIS_URL=redis://localhost:6379

# external APIs
# New Anthropic key goes here. Never commit. Server-side only.
ANTHROPIC_API_KEY=
# Google Maps key is shared with client via /config endpoint in later phases.
GOOGLE_MAPS_API_KEY=

# auth (Phase 3)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
