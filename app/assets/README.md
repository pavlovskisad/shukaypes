# Assets

Asset package lands here during Phase 2 (contract designer delivers companion sprites, skins, UI kit per the product doc).

Expected subdirs:
- `fonts/` — Caveat-Regular.ttf, DMSans-Regular.ttf
- `logos/` — hand-drawn white dog in rounded square (full, mark, icon variants)
- `companion/` — squiggle/calligraphy dog silhouette for the map overlay

Reference assets (base64-embedded) live in `/reference/shukajpes-demo.html`.
